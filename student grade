#include <stdio.h>

int main()
{
  int marks;
  marks = 64;

  if(marks >= 90)
    printf("A+");
  else if(marks >= 80)
    printf("A");
  else if(marks >= 70)
    printf("B");
  else if(marks >= 50)
    printf("C");
  else if(marks >= 40)
    printf("D");
  else
    printf("F");

  return 0;
}
